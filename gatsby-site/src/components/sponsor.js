import * as React from "react"
import "./layout.scss"

export default function SponsorCom() {
  return (
    <section>
      <h1 className="text-6xl font-bold text-neutral-700">Sponsor</h1>
      <div className="service-wrap container-blo">
        <p>I'm creating pages programmatically in gatsby from MD files. the issues I'm having is I'm using the from gatsby-plugin-image to pull the image from the frontmatter of said MD file when the page loads the img is not rendered and i get and error gatsby-plugin-image] Missing image prop</p>
      </div>
    </section>

  )
}
